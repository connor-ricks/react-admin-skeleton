import IMenuItem from '@models/menu-item';
/**
 * Represents the menu in the dashboard.
 * @interface
 */
export default class IMenu {
  /**
   * The menu's items.
   * @type {Array<IMenuItem>}}
   */
  items;
}
